# Secondary-index contract

Each exported index has one documented formulation, input units, applicability range, return unit and scientific citation. Do not expose software-specific aliases or silently substitute a different formula variant.

Invalid-domain handling and numerical tolerances are part of each index contract.
